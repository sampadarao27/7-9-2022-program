#include <stdio.h>

int main()
{
    int *p;
    int i,n,sum=0;
    printf("enter you range");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    { 
        scanf("%d",(p+i));
    }
    for(i=0;i<n;i++)
    { 
        sum=sum+*(p+i);
    }
  printf("%d",sum);
    return 0;
}
